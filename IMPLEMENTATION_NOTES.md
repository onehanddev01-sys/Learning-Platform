# IMPLEMENTATION_NOTES — บันทึกการพัฒนาระบบจริง

> เอกสารนี้สรุปวิธีดำเนินการพัฒนา "เว็บแอปพลิเคชันสื่อการเรียนรู้การเขียนโปรแกรม Python สำหรับผู้เริ่มต้น"
> เขียนไว้ละเอียดพอสำหรับนำไปเรียบเรียงเป็น "บทที่ 3 วิธีดำเนินโครงงาน" ของเล่มโครงงาน

---

## 1. โครงสร้างไฟล์จริงของระบบ

```
/
├── index.html            หน้า Welcome (ปุ่มหลักปรับตามความก้าวหน้า: เริ่มต้นเรียนรู้ / เรียนต่อจากที่ค้างไว้ / ทบทวนบทเรียน)
├── map.html              หน้าแผนที่ภารกิจ (Mission Map)
├── mission.html          หน้าบทเรียน — ใช้ร่วมกันทุกด่านผ่าน ?id= (m1..m15, mp1..mp4, cap)
├── test.html             หน้า self-test สำหรับผู้พัฒนา (ไม่มีลิงก์จากหน้าเว็บจริง)
├── css/
│   └── style.css         สไตล์ทั้งเว็บ (ธีมภารกิจอวกาศแบบสะอาดตา)
├── js/
│   ├── runner.js         ห่อ Skulpt: run(code) → {output, error, timedOut, truncated, interrupted}
│   ├── checker.js        Pass/Fail Engine ตรวจ 2 ชั้น
│   ├── errors.js         ระบบแปล Error เป็นภาษาไทย + catalog กลาง
│   ├── hints.js          ระบบคำใบ้ 3 ระดับ + เปิดอัตโนมัติเมื่อ Error เดิมซ้ำ
│   ├── progress.js       localStorage + กติกาปลดล็อก + in-memory fallback
│   ├── app.js            ตัวควบคุมหน้า mission (เรนเดอร์เนื้อหา, ปุ่ม Run/Hint, auto-save)
│   └── map.js            เรนเดอร์แผนที่ภารกิจ + ปุ่มเริ่มใหม่ทั้งหมด
├── data/
│   └── missions.js       เนื้อหาบทเรียนทั้งหมด 20 ด่าน (ข้อมูลล้วน แยกจากตรรกะระบบ 100%)
├── vendor/               สำเนาไลบรารีสำหรับใช้งานออฟไลน์ (fallback ของ CDN)
│   ├── skulpt.min.js
│   ├── skulpt-stdlib.js
│   └── codemirror/ (codemirror.min.js, codemirror.min.css, python.min.js)
└── IMPLEMENTATION_NOTES.md (ไฟล์นี้)
```

หลักการ **แยกข้อมูลออกจากตรรกะ (Separation of Data and Logic)**: เนื้อหาบทเรียนทั้งหมด
(Hook, โค้ดตัวอย่าง, คำอธิบาย, โจทย์, คำใบ้, เกณฑ์ตรวจ, เฉลย) อยู่ใน `data/missions.js` ไฟล์เดียว
แก้เนื้อหา/เพิ่มด่านได้โดยไม่ต้องแตะโค้ดระบบแม้แต่บรรทัดเดียว

## 2. เวอร์ชันไลบรารีและวิธีโหลด

| ไลบรารี | เวอร์ชัน | แหล่งหลัก (CDN) | สำรอง |
|---|---|---|---|
| Skulpt | 1.2.0 | jsDelivr `skulpt@1.2.0/dist/skulpt.min.js` + `skulpt-stdlib.js` | `vendor/` |
| CodeMirror | 5.65.16 | jsDelivr `codemirror@5.65.16` (lib + mode/python + css) | `vendor/codemirror/` |
| ฟอนต์ Sarabun | Google Fonts | fonts.googleapis.com | ฟอนต์ระบบ (Leelawadee UI / Tahoma) |

**กลยุทธ์ CDN-first + local fallback:** ทุกหน้าโหลดไลบรารีจาก CDN ก่อน แล้วเช็คว่า global object
(`window.Sk`, `window.CodeMirror`) ถูกสร้างหรือยัง ถ้าไม่ (เช่น เปิดแบบ file:// ตอนออฟไลน์)
จะโหลดสำเนาจาก `vendor/` แทนด้วย `document.write` ทำให้เปิดใช้งานได้ครบทั้ง 3 แบบ:
เปิดไฟล์ index.html ตรง ๆ / GitHub Pages / Vercel — และยังทำงานได้แม้ไม่มีอินเทอร์เน็ต
(ยกเว้นฟอนต์ Google จะ fallback เป็นฟอนต์ไทยของระบบ)

**ผล Smoke Test ของ Skulpt 1.2.0 (ทดสอบก่อนเขียนเนื้อหา — ผ่านครบ 19/19):**
`print`, ตัวแปร, **f-string (รวม `{a+b}` และ `{pi:.2f}`)**, `%`, `int()/float()/str()`,
`input()` (แบบ async suspension), `if/elif/else`, `for/range`, `while`, `list`, `def/return`,
และ Error ทุกชนิดที่ต้องแปล (ZeroDivision, Name, Type, Syntax, TimeLimit)
→ ไม่ต้องเปลี่ยน build ตามที่ spec เผื่อไว้

## 3. สถาปัตยกรรมการทำงาน

ลำดับการทำงานเมื่อผู้เรียนกด Run (ทั้งหมดเกิดในเบราว์เซอร์ ไม่มีเซิร์ฟเวอร์):

```
CodeMirror (โค้ดผู้เรียน)
   → runner.js: Sk.configure + importMainWithBody
       - output สตรีมสดเข้า Output Panel (buffer ผ่าน requestAnimationFrame)
       - input() → แสดงช่องกรอกใน Output Panel (Sk.inputfun แบบ Promise)
       - execLimit 3,000 ms กัน while วนไม่จบ
       - เพดาน output 2,000 บรรทัด กันเบราว์เซอร์ค้าง
   → ชั้นที่ 1: มี Error?
       ใช่ → errors.js แปลไทย (override ของด่าน → catalog → fallback)
            + ไฮไลต์บรรทัดใน editor (แดงอ่อน)
            + hints.js นับ Error ซ้ำ → เปิดคำใบ้อัตโนมัติเมื่อครบกำหนด
   → ชั้นที่ 2: รันสำเร็จ → checker.js ตรวจ rules ของด่าน (AND ทุกข้อ)
       ผ่าน → Win Message + progress.js บันทึก + ปลดด่านถัดไป
       ไม่ผ่าน → failMessage โทนส้ม + นับใน streak เดียวกับ Error (ชนิด "CheckFail")
```

## 4. ตารางเกณฑ์การตรวจ (check rules) ของทุกด่าน

รูปแบบกติกาที่ engine รองรับ: `codeChanged`, `outputContains`, `outputEquals`, `outputMatches`,
`outputLineCountAtLeast` (นับเฉพาะบรรทัดที่ไม่ว่าง), `codeIncludes`, `codeNotIncludes`, `codeMatches`
— กติกาฝั่ง code ทุกตัวตรวจกับโค้ดที่ **ตัดคอมเมนต์ออกแล้ว** (กันคำในคอมเมนต์ของ starterCode ทำให้ผ่านโดยบังเอิญ)
และ **ทุกด่านมี `codeChanged`** (โค้ดต้องต่างจากตั้งต้นและไม่ว่าง) กันการกด Run เฉย ๆ แล้วผ่าน

| ด่าน | เกณฑ์ผ่าน (AND ทุกข้อ) | testInputs |
|---|---|---|
| m1 | codeChanged + outputContains "ฉันเขียนโปรแกรมได้แล้ว" + output ≥ 2 บรรทัด | — |
| m2 | codeChanged + codeMatches `food\s*=\s*['"].+['"]` + output ≥ 1 บรรทัด | — |
| m3 | codeChanged + codeMatches `input\([\s\S]*input\(` (ถาม 2 ครั้ง) + codeIncludes `print(` + output ≥ 2 | สมชาย, ฟ้า |
| mp1 | codeChanged + input 2 ครั้ง + codeIncludes `print(` + output ≥ 2 | สมชาย, อ่านหนังสือ |
| m4 | codeChanged + codeIncludes `int(` + outputContains "ปีหน้าคุณจะอายุ" | 17 |
| m5 | codeChanged + outputMatches `^4$`, `^21$`, `^2\.33`, `^1$` (ผลของ - * / % จาก a=7, b=3) | — |
| m6 | codeChanged + codeMatches `print\s*\(\s*f['"]` + มีปีกกา 2 คู่ในบรรทัดเดียว + output ≥ 1 | — |
| mp2 | codeChanged + codeIncludes `float(` + codeMatches `f['"]` + outputContains "BMI" | 60, 1.65 |
| m7 | codeChanged + codeIncludes `if ` + outputContains "ผ่าน" | — |
| m8 | codeChanged + codeIncludes `else` + codeIncludes "รหัสไม่ถูกต้อง" + output ≥ 1 | abc |
| m9 | codeChanged + codeIncludes `elif` + `else` + outputContains "เกรด" | 75 |
| mp3 | codeChanged + codeIncludes `input(` + `elif` + `else` + output ≥ 1 | 66 |
| m10 | codeChanged + codeMatches `for\s+\w+\s+in\s+range` + output ≥ 10 บรรทัด + outputContains "10" | — |
| m11 | codeChanged + codeIncludes `while` + รันจบไม่ timeout + outputContains "ปล่อยจรวด" และ "1" | — |
| m12 | codeChanged + codeMatches `\[` + codeIncludes `for ` + output ≥ 3 บรรทัด | — |
| mp4 | codeChanged + codeIncludes `for ` + codeNotIncludes "40" (กันฮาร์ดโค้ดคำตอบ) + outputContains "40" และ "8.0" | — |
| m13 | codeChanged + codeIncludes `def ` + output ≥ 2 บรรทัด (พิสูจน์ว่าเรียกใช้ 2 ครั้ง) | — |
| m14 | codeChanged + codeIncludes `return` + outputContains "10" + codeNotIncludes `print(10` | — |
| m15 | codeChanged + codeIncludes `def ` + codeMatches `\b(if|elif)\b` + codeIncludes `for ` + codeMatches `\[` + output ≥ 3 | — |
| cap | **ตามเอกสารเป๊ะ:** codeChanged + รันได้ไม่มี Error + output ≥ 1 บรรทัด (ไม่ตัดสินเนื้อหา) | 20, 60 |

หมายเหตุด้านการสอนที่ฝังในโค้ดตั้งต้น:
- **m4 (Productive Failure):** starterCode มีบั๊กตั้งใจ (`age = input(...)` แล้วเอาไป `+ 1`) → เกิด TypeError ให้ผู้เรียนแก้เอง
  พร้อม `errorOverrides.TypeError` ข้อความเฉพาะด่าน และ `hintTriggerRepeats: 3` (ช้ากว่าด่านอื่นที่ใช้ 2)
- **m6 (Just-in-Time):** showCode แสดงการต่อข้อความด้วย `+`/`str()` ที่ยุ่งยากก่อน แล้วเทียบ f-string ในโค้ดเดียวกัน
- **m9:** ใช้บริบทตัดเกรด A B C / **m12 (Retrieval Practice):** บังคับใช้ for จากด่าน 10 ร่วมกับ list
- **m11:** starterCode เป็นลูปวนไม่จบโดยตั้งใจ ให้ผู้เรียนเจอ timeout ในที่ปลอดภัยแล้วเติมบรรทัดหยุดลูปเอง
- **Scaffolding ลดลงตามช่วง:** m1–m3 แก้จากโค้ดเกือบสมบูรณ์ → m4–m9 โครงร่าง+คอมเมนต์นำทาง → m10–m15 โจทย์เป็นคำอธิบาย โค้ดตั้งต้นแทบไม่มี → cap ไม่มีโค้ดตั้งต้นเลย

## 5. localStorage Schema จริง

Key: `pylearn_progress_v1` เก็บ JSON ก้อนเดียว:

```json
{
  "version": 1,
  "completed": ["m1", "m2", "mp1"],
  "currentMissionId": "m3",
  "capstoneDone": false,
  "savedCode": { "m3": "โค้ดล่าสุดที่พิมพ์ค้างไว้ของแต่ละด่าน" },
  "hintState": { "m3": 1 },
  "errorTracking": { "m3": { "lastType": "NameError", "streak": 1 } },
  "updatedAt": "2026-07-02T11:30:00.000Z"
}
```

- **Auto-save ไม่มีปุ่มบันทึก** — บันทึกเมื่อ: กด Run ทุกครั้ง / ผ่านด่าน / ออกจากหน้า (`beforeunload`) / สลับแท็บ (`visibilitychange`)
- กลับเข้าด่านเดิม → โค้ดล่าสุดกลับมาใน editor, ระดับ Hint ที่เปิดแล้วยังเปิดอยู่ / เข้าครั้งแรก → starterCode
- ผ่านซ้ำไม่ duplicate (เช็คก่อน push) / ผ่านแล้วล้าง errorTracking ของด่านนั้น
- ข้อมูลเสียรูป (parse ไม่ได้ / version ไม่ตรง) → เริ่ม state ใหม่ ไม่ crash
- **localStorage ใช้ไม่ได้** (โหมดส่วนตัว/เต็ม) → เก็บในหน่วยความจำแทน + แบนเนอร์แจ้งสุภาพว่าปิดแท็บแล้วข้อมูลหาย
- **กติกาปลดล็อก:** เรียงเป็นเส้นตรงตามลำดับใน MISSIONS (ด่านปลดเมื่อด่านก่อนหน้าผ่าน) — ดูเหตุผลในข้อ 8.1
- **ปุ่ม "ลบความคืบหน้า"** อยู่บนการ์ด "ความก้าวหน้าของคุณ" ในหน้าแผนที่ กดแล้วมี modal
  ยืนยัน 2 ชั้น โดยจงใจ **สลับตำแหน่งปุ่มยืนยัน**: ชั้นแรกปุ่มยืนยันอยู่ซ้าย ชั้นที่สองย้ายไปขวา
  — ถ้าผู้ใช้กดรัว ๆ ที่ตำแหน่งเดิมโดยไม่อ่าน จะไปเจอปุ่มยกเลิกแทน กันการลบโดยไม่ตั้งใจ
  ยืนยันครบสองชั้นจึงลบ state ทั้งหมดแล้วเริ่มใหม่ตั้งแต่ด่านแรก

## 6. ระบบแปล Error (errors.js)

**รูปแบบข้อความ 3 ส่วน:** เกิดอะไรขึ้น (ภาษาคน) → อยู่ตรงไหน (บรรทัดที่ X ดึงจาก traceback ของ Skulpt) → แนวทางแก้ (ชี้ทาง ไม่เฉลย)
แสดงเป็นกล่อง **พื้นส้มอ่อน/ตัวอักษรส้ม** ใน Output Panel ตำแหน่งเดียวกับผลรันปกติ
Error ดิบภาษาอังกฤษถูกพับเก็บใน "ดูข้อความต้นฉบับ" (ไม่แสดงเป็นค่าเริ่มต้น)
พร้อมไฮไลต์บรรทัดที่ผิดใน editor ด้วยพื้นแดงอ่อน (จุดเดียวที่ใช้โทนแดงได้ตามเอกสาร)

**ลำดับการเลือกข้อความ:** `errorOverrides` ของด่าน → catalog กลาง → fallback ทั่วไป

**Catalog กลางครอบคลุม 13 ชนิด:**

| ชนิด | ครอบคลุมกรณี |
|---|---|
| SyntaxError | แยกย่อย 6 กรณีด้วยการวิเคราะห์โค้ดจริงประกอบ: (1) ลืมปิดเครื่องหมายคำพูด — สแกนหาบรรทัดที่ quote ค้าง (2) ใช้ `=` ตัวเดียวในเงื่อนไข if/elif/while แทน `==` (3) ลืม `:` ท้าย if/for/while/def — ตรวจบรรทัดที่ error (4) ลืมย่อหน้าหลังบรรทัดที่จบด้วย `:` (5) วงเล็บเหลี่ยม `[ ]` ของลิสต์ไม่ครบคู่ (6) วงเล็บ `( )` ไม่ครบคู่ + fallback ทั่วไป |
| IndentationError | ย่อหน้าไม่ถูกต้อง |
| NameError | ดึงชื่อตัวแปรจากข้อความดิบมาใส่ในคำอธิบาย (เรียกตัวแปรก่อนสร้าง/สะกดผิด) |
| TypeError | แยกย่อย 3 กรณี: (1) ใส่วงเล็บ ( ) หลังสิ่งที่ไม่ใช่ฟังก์ชัน เช่น `mylist(0)` — ถ้าเป็น list จะแนะนำให้ใช้ `mylist[0]` (2) เรียกฟังก์ชันด้วยจำนวน argument ไม่ตรงกับที่นิยาม — ดึงชื่อฟังก์ชันมาใส่ในคำอธิบาย (3) str + int พร้อมทางแก้ทั้งฝั่งคำนวณ (int/float) และฝั่งต่อข้อความ (str) + fallback ทั่วไป |
| ValueError | int("ข้อความ") แปลงไม่ได้ |
| ZeroDivisionError | หารด้วยศูนย์ |
| IndexError | index เกินขนาดลิสต์ + เตือนว่าเริ่มนับที่ 0 |
| TimeLimitError | ลูปวนไม่จบ → "โค้ดอาจกำลังวนซ้ำไม่รู้จบ ลองตรวจดูว่าเงื่อนไขของ while มีทางเป็นเท็จไหม" |
| ImportError / ModuleNotFoundError | หาโมดูลไม่เจอ — ดึงชื่อโมดูลจากข้อความดิบ + แจ้งว่าหลักสูตรนี้ไม่ต้องใช้ import และบอกโมดูลที่ Skulpt รองรับ (math, random, time) |
| UnboundLocalError | ใช้ตัวแปรในฟังก์ชันก่อนบรรทัดที่กำหนดค่า — ดึงชื่อตัวแปรมาใส่ในคำอธิบาย |
| KeyError | เรียก dictionary ด้วยคีย์ที่ไม่มีอยู่ — ดึงชื่อคีย์มาใส่ในคำอธิบาย |
| RecursionError / AttributeError | ข้อความทั่วไปแบบเป็นมิตร |

เหตุผลที่ต้องวิเคราะห์โค้ดเอง: ข้อความดิบของ Skulpt กำกวม เช่น quote ค้าง = `bad token T_OP`,
วงเล็บค้าง = `EOF in multi-line statement`, ส่วนลืม colon / ลืมย่อหน้า / วงเล็บเหลี่ยมค้าง /
ใช้ `=` ในเงื่อนไข ทั้งหมดรายงานเป็น `bad input` เหมือนกันหมด
จึงเขียน heuristic ตรวจจากโค้ดผู้เรียนโดยตรงเพื่อชี้สาเหตุที่แท้จริง

ขอบเขตที่ตั้งใจไม่รวม (โอกาสเจอในเว็บนี้ต่ำมาก): FileNotFoundError, OverflowError,
StopIteration, TabError (editor บังคับ Tab เป็น 4 ช่องว่างอยู่แล้ว) — กรณีเหล่านี้ตกไปที่
fallback ทั่วไปซึ่งยังเป็นภาษาไทยที่เป็นมิตรอยู่ดี

**errorOverrides ที่ใช้จริง:** ด่าน m4 — TypeError → "เจอกับดักแล้ว! ตอนนี้ age เป็น 'ข้อความ' อยู่ …"

## 7. ระบบคำใบ้ (hints.js)

- 3 ระดับต่อด่าน: กระตุ้นคิด → ชี้จุด+เครื่องมือ → เกือบเฉลย / กดขอเองได้ตลอด ไม่มีการหักคะแนน
- **ปุ่มที่ Task Bar แยกหน้าที่ "ดู" กับ "ขอ" ออกจากกัน:**
  - ยังไม่เคยมีคำใบ้ (ระดับ 0) → ปุ่มชื่อ "ขอคำใบ้" กดแล้วเปิดคำใบ้ระดับ 1
  - มีคำใบ้เปิดไว้แล้ว (ขอเองหรือระบบเปิดให้) → ปุ่มเปลี่ยนชื่อเป็น "ดูคำใบ้"
    กดแล้วแค่เปิดแผงดูของเดิม **ไม่เพิ่มระดับ** — ปิดแผงแล้วกลับมาเปิดกี่ครั้งก็ได้
  - อยากได้ระดับถัดไปจริง ๆ ต้องกดปุ่ม "ขอคำใบ้เพิ่ม" ที่อยู่ในแผงเท่านั้น
- **Auto-reveal:** ไม่ผ่าน "ชนิดเดิม" ซ้ำติดกันครบ `hintTriggerRepeats` (ค่าเริ่มต้น 2, ด่าน 4 = 3)
  → เปิดคำใบ้ระดับถัดไปให้เอง พร้อมข้อความ "ดูเหมือนจะติดตรงนี้อยู่ นี่คือคำใบ้เพิ่มเติมที่อาจช่วยได้"
  แล้วรีเซ็ตตัวนับ (กันเปิดรัว) / ไม่ผ่านคนละชนิด = เริ่มนับใหม่
- กดครบ 3 ระดับแล้วกดอีก → "นี่คือคำใบ้สุดท้ายแล้ว สู้ ๆ …"
- ระดับที่เปิดแล้วบันทึกใน `hintState` — refresh แล้วคำใบ้ยังเปิดอยู่ และป้ายปุ่มยังเป็น "ดูคำใบ้"

## 7.1 พฤติกรรมหลังผ่านด่าน (โหมดทบทวน)

- **Win overlay (หน้าต่างภารกิจสำเร็จ) เด้งเฉพาะ "ครั้งแรก" ที่ผ่านด่านนั้น** —
  หลังจากนั้นไม่ว่าจะกด "อยู่ทบทวนต่อ" แล้วรันผ่านซ้ำ หรือกลับเข้ามาเล่นใหม่ภายหลัง
  จะไม่มี overlay มากวนอีก การรันผ่านซ้ำจะได้กล่องข้อความสีเขียวเล็ก ๆ ใน Output Panel แทน
  ("ยังผ่านภารกิจอยู่ เยี่ยม! ทดลองแก้โค้ดเล่นต่อได้เต็มที่ …")
- **ปุ่ม "ด่านถัดไป" ติดอยู่ที่ Task Bar** ตลอดเวลาเมื่อด่านนั้นมีสถานะผ่านแล้ว
  (ทั้งตอนกดอยู่ทบทวนต่อ และตอนกลับเข้ามาเล่นด่านที่ผ่านแล้ว) ผู้เรียนจึงทดลอง
  แก้โค้ดเล่นได้เรื่อย ๆ แล้วค่อยไปต่อเมื่อพร้อม — ถ้าด่านถัดไปคือ Capstone
  ป้ายปุ่มจะเป็น "ไปที่ Capstone" ส่วนด่านสุดท้าย (cap) ไม่มีปุ่มนี้

## 8. การตัดสินใจที่ทำแทน spec (Decision Log)

1. **ปลดล็อกแบบเส้นตรง:** เอกสารกำหนดกติกาแยกราย Module/Mini Project/Capstone แต่เมื่อจัดลำดับใน
   missions.js ให้ Mini Project ต่อท้าย 3 Mission ของ Module และ cap อยู่ท้ายสุด กติกา "ปลดเมื่อด่านก่อนหน้าผ่าน"
   ให้ผลเทียบเท่าทุกข้อ จึงใช้แบบเส้นตรงเพื่อลดความซับซ้อน
2. **CDN-first + vendor fallback:** spec ให้โหลดจาก CDN และให้เปิดแบบ file:// ได้ — ทำทั้งสองอย่างพร้อมกัน
3. **การตรวจไม่ผ่าน (CheckFail) นับใน streak เดียวกับ Error:** เอกสารพูดถึง "Error เดิมซ้ำ" — ตีความรวมกรณี
   รันได้แต่ไม่ผ่านเกณฑ์ด้วย เพราะผู้เรียนติดเหมือนกัน ควรได้คำใบ้เหมือนกัน
4. **ตัดคอมเมนต์ก่อนตรวจกติกาฝั่ง code:** กันกรณีคอมเมนต์นำทางใน starterCode (เช่น "# เพิ่ม else …")
   ทำให้ `codeIncludes: "else"` ผ่านทั้งที่ยังไม่ได้เขียน (ตัวตัดคอมเมนต์รู้จัก string ปกติ แต่ไม่รองรับ triple-quoted
   ซึ่งอยู่นอกขอบเขตหลักสูตร)
5. **ปุ่ม "หยุดการทำงาน" ระหว่างรอ input:** เผื่อผู้เรียนกดรันโค้ดที่มี input แล้วไม่อยากพิมพ์ต่อ จะได้ไม่ค้าง
6. **รีเซ็ตนาฬิกา execLimit หลังตอบ input:** ไม่ให้เวลาที่ผู้เรียนใช้พิมพ์คำตอบถูกนับเป็นเวลารันจนโดน timeout ฟรี ๆ
7. **Output buffer ผ่าน requestAnimationFrame:** ลง DOM เป็นชุดแทนทีละ print ทำให้ loop หมื่นรอบไม่ทำให้อืด
   และถึงเพดาน 2,000 บรรทัดได้จริง (ตัดแล้วแจ้ง ไม่ถือเป็น Error)
8. **mp4 ตรวจเข้มกว่า spec เล็กน้อย:** เพราะโจทย์ล็อกลิสต์ให้แล้ว จึงตรวจค่าจริง (40, 8.0) + `codeNotIncludes "40"`
   กันพิมพ์คำตอบตรง ๆ / m14 ก็กัน `print(10` เช่นกัน
9. **m5 ใช้ outputMatches แบบต้นบรรทัด-ท้ายบรรทัด** (`^4$` ฯลฯ) เพราะ outputContains "1" เฉย ๆ จะชนกับ "21"
10. **Editor fallback:** ถ้า CodeMirror โหลดไม่สำเร็จ ใช้ textarea ธรรมดาแทน (ยังเรียนต่อได้ ไม่ crash)
11. **ชื่อเว็บ:** "Python เริ่มจากศูนย์" — สื่อกลุ่มเป้าหมายที่ไม่เคยเขียนโปรแกรมมาก่อน
12. **ธีม:** โครงสีน้ำเงินเข้มอวกาศ + พื้นที่ทำงานสว่าง + Output Panel แบบคอนโซลมืด — เข้าธีม "First Contact"
    โดยไม่มีของตกแต่งรบกวนสมาธิ (เลี่ยง seductive details) และไม่ใช้อีโมจิใด ๆ ทั้งเว็บ (ใช้ไอคอน SVG)
13. **สิ่งที่จงใจไม่มี (ตามเอกสาร):** leaderboard, คะแนน/แต้ม, จับเวลา, ตัวเลขรวมความก้าวหน้า (ใช้จุด dot),
    การโชว์จำนวนครั้งที่ผิด, ระบบแข่งขันทุกชนิด
14. **ปุ่มคำใบ้แยก "ดู" กับ "ขอ":** ปุ่มหลักที่ Task Bar ใช้เปิดดูคำใบ้ที่มีอยู่ (ไม่เพิ่มระดับ)
    การขอระดับถัดไปทำได้จากปุ่ม "ขอคำใบ้เพิ่ม" ในแผงเท่านั้น — แก้ปัญหาผู้เรียนที่แค่อยากเปิด
    กลับมาอ่านคำใบ้เดิมแต่โดนระบบเผลอเปิดระดับใหม่ให้
15. **Win overlay ครั้งแรกเท่านั้น:** ผ่านด่านแล้วรันผ่านซ้ำในโหมดทบทวนจะได้กล่องเขียวเล็ก ๆ แทน
    ไม่เด้งเต็มจอรบกวนการทดลองเล่น และมีปุ่ม "ด่านถัดไป" ค้างที่ Task Bar แทน
16. **ปุ่มลบความคืบหน้าย้ายมาไว้บนการ์ดความก้าวหน้า** (เดิม spec ให้ซ่อนมุมเล็ก ๆ) พร้อม modal
    ยืนยัน 2 ชั้นแบบสลับข้างปุ่มยืนยัน (ซ้าย → ขวา) กันกดรัวโดยไม่อ่าน — ทำตามคำขอของเจ้าของโครงงาน
17. **ขอบเขต Error catalog:** ขยายให้ครอบคลุม Error ที่มีโอกาสเจอจริงบนเว็บนี้ (รวม import,
    UnboundLocal, KeyError, เรียกสิ่งที่ไม่ใช่ฟังก์ชัน, argument ไม่ครบ, `=` ในเงื่อนไข, `[ ]` ไม่ครบ)
    แต่ไม่รวมชนิดที่แทบไม่มีทางเจอ (เช่น FileNotFoundError) ซึ่งจะตกไปที่ fallback ภาษาไทยแบบทั่วไป

## 9. ผลการทดสอบ (Definition of Done)

**9.1 test.html (dev harness)** — รันอัตโนมัติทุกด่าน: เฉลยผ่าน checker + โค้ดตั้งต้นไม่ผ่าน
ผลล่าสุด: **ผ่านครบ 20/20 ด่าน** (15 Mission + 4 Mini Project + 1 Capstone)
ข้อควรรู้: harness ต้องรันทีละด่านตามลำดับ เพราะ Skulpt ใช้ config กลางร่วมกัน รันขนานแล้วผลเพี้ยน

**9.2 Edge cases ทั้ง 14 ข้อ — ทดสอบผ่านครบ:**

| # | กรณี | ผล |
|---|---|---|
| 1 | Run โดยไม่แก้ starterCode | ไม่ผ่าน + ข้อความชวนลงมือ (rule codeChanged) |
| 2 | Run โค้ดว่าง | ข้อความเป็นมิตร ไม่รัน ไม่ crash |
| 3 | while วนไม่จบ | timeout ~3 วิ + ข้อความแนะนำ + ปุ่ม Run กลับมาใช้ได้ |
| 4 | input() แล้วกด Enter เปล่า | ได้ค่าว่าง โปรแกรมทำงานต่อตามธรรมชาติ ไม่ค้าง |
| 5 | Refresh กลางด่าน | โค้ดล่าสุดกลับมา + Hint ที่เปิดแล้วยังเปิดอยู่ |
| 6 | URL ตรงเข้าด่านที่ล็อก | redirect กลับ Map + ข้อความแจ้ง |
| 7 | id ไม่มีจริง | redirect กลับ Map + ข้อความแจ้ง |
| 8 | localStorage ถูกปิด/เต็ม | in-memory fallback + แบนเนอร์ ไม่ crash |
| 9 | ผ่านด่านเดิมซ้ำ | completed ไม่ duplicate ปลดล็อกไม่เพี้ยน |
| 10 | กด Run รัว ๆ | ปุ่ม disabled ระหว่างรัน + สถานะ "กำลังรัน…" |
| 11 | print 10,000 รอบ | ตัดที่ 2,000 บรรทัด + แจ้งเตือน + ไม่ freeze |
| 12 | ภาษาไทยใน print/input | แสดงถูกต้องทุกจุด |
| 13 | ขอ Hint เกิน 3 ระดับ | "นี่คือคำใบ้สุดท้ายแล้ว สู้ ๆ" |
| 14 | เข้าครั้งแรกไม่มี state | ทุกหน้าทำงาน m1 ปลดด่านเดียว จุด progress ว่าง |

**9.3 เล่นจริงด้วยมือ (ผ่าน browser automation):** ไล่ m1 → m2 → m3 (รวม flow input) ครบ Module 1,
ด่าน m4 ครบวงจร Productive Failure (Error override → Hint อัตโนมัติครั้งที่ 3 → แก้แล้วผ่าน),
m11 timeout → แก้ → ผ่าน, Capstone (โจทย์อิสระ + จบหลักสูตร), แผนที่ทั้ง 3 สถานะ, ปุ่มเริ่มใหม่ยืนยัน 2 ชั้น

**9.4 การเผยแพร่:** ระบบเป็น static ล้วน — เปิด index.html ตรง ๆ ได้ / อัปโหลดขึ้น GitHub Pages หรือ
Vercel ได้ทันทีโดยไม่ต้องตั้งค่า build ใด ๆ (โฟลเดอร์ `.claude/` เป็นไฟล์ช่วยพัฒนา ไม่เกี่ยวกับตัวเว็บ
จะลบก่อน deploy ก็ได้)
