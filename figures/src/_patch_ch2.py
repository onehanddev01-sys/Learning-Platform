# -*- coding: utf-8 -*-
# แพตช์บทที่ 2: ขยายเนื้อหาเทคโนโลยี + แทรกภาพ/ตาราง + เลขแบบ 2.x + คำบรรยายภาพใต้ภาพ
import io, re, sys

f = 'บทที่2_เอกสารและงานวิจัยที่เกี่ยวข้อง.md'
s = io.open(f, encoding='utf-8').read()

def fig(num, cap, path):
    return f'![ภาพที่ {num} {cap}]({path})\n\n**ภาพที่ {num} {cap}**'

def ph(num, cap, note):
    return f'*(ผู้จัดทำหารูปเอง: {note})*\n\n**ภาพที่ {num} {cap}**'

def tab(num, cap, path):
    return f'**ตารางที่ {num} {cap}**\n\n![ตารางที่ {num} {cap}]({path})'

INS = []  # (anchor_suffix, text_to_append_after_paragraph)

INS.append(('ซึ่งช่วยทั้งการจัดวางด้วย CSS และการเข้าถึงสำหรับผู้ใช้เทคโนโลยีช่วยการเข้าถึง',
"\n\nองค์ประกอบ HTML5 หลักที่โครงงานใช้และหน้าที่ของแต่ละองค์ประกอบสรุปดังตารางที่ 2.3\n\n"
+ tab('2.3', 'องค์ประกอบ HTML5 หลักที่ใช้ในโครงงานและหน้าที่ของแต่ละองค์ประกอบ', 'figures/table-2-html.png')
+ "\n\nโครงสร้างจริงของหน้าบทเรียนซึ่งจัดวางด้วยองค์ประกอบเชิงความหมายข้างต้นแสดงดังภาพที่ 2.4 จะเห็นว่าแต่ละส่วนของหน้าจอถูกประกาศด้วยแท็กที่บอกบทบาทของตนเอง ทำให้โค้ดอ่านเข้าใจได้และจัดวางต่อด้วย CSS ได้ทันที\n\n"
+ fig('2.4', 'โครงสร้างเอกสาร HTML5 ของหน้าบทเรียน (mission.html)', 'figures/code-html-structure.png')))

INS.append(('ก็ถูกกำหนดรูปแบบผ่าน CSS ตามเจตนาที่ไม่ต้องการให้ผู้เรียนท้อกับระยะทางที่เหลือ',
"\n\nคุณสมบัติของ CSS3 ที่โครงงานใช้จริงและจุดที่ใช้สรุปดังตารางที่ 2.4\n\n"
+ tab('2.4', 'คุณสมบัติ CSS3 ที่ใช้ในโครงงานและจุดที่ใช้งาน', 'figures/table-2-css.png')
+ "\n\nตัวอย่างโค้ด CSS จริงของโครงงานที่แสดงการใช้ตัวแปร CSS รวมศูนย์ชุดสีของทั้งระบบไว้ที่เดียว และการใช้ Media Query ปรับผังหน้าจอตามความกว้างของจอ แสดงดังภาพที่ 2.5\n\n"
+ fig('2.5', 'ตัวอย่างการใช้ตัวแปร CSS และ Media Query ในไฟล์ style.css ของโครงงาน', 'figures/code-css-vars.png')))

JS = """

เนื่องจาก JavaScript เป็นเทคโนโลยีหลักที่สุดของโครงงาน หัวข้อนี้จึงอธิบายไวยากรณ์พื้นฐานของภาษาที่โครงงานใช้จริง พร้อมตัวอย่างโค้ดที่ตัดมาจากระบบโดยตรง เพื่อให้เห็นว่าแนวคิดแต่ละเรื่องถูกใช้งานจริงอย่างไร ภาพรวมของไวยากรณ์ที่ใช้สรุปดังตารางที่ 2.5

""" + tab('2.5', 'ไวยากรณ์พื้นฐานของ JavaScript ที่โครงงานใช้ พร้อมตัวอย่างจริงจากระบบ', 'figures/table-2-js.png') + """

1) ตัวแปรและชนิดข้อมูล (Variables and Data Types) — JavaScript ประกาศตัวแปรด้วยคำสั่ง var โดยไม่ต้องระบุชนิดข้อมูลล่วงหน้า ชนิดข้อมูลที่โครงงานใช้ประกอบด้วยข้อความ (String) ตัวเลข (Number) ค่าความจริง (Boolean) อาร์เรย์ (Array) สำหรับข้อมูลที่เป็นรายการ และวัตถุ (Object) สำหรับข้อมูลที่มีโครงสร้างเป็นคู่ชื่อกับค่า โครงงานเลือกใช้ var แทน let/const เพื่อให้โค้ดทำงานได้บนเบราว์เซอร์รุ่นเก่าในห้องคอมพิวเตอร์ของสถานศึกษา ตัวอย่างจริงจากโมดูลติดตามความก้าวหน้าแสดงดังภาพที่ 2.6 ซึ่งเห็นการใช้ชนิดข้อมูลครบทุกชนิดในโครงสร้างสถานะเริ่มต้นของผู้เรียน

""" + fig('2.6', 'การประกาศตัวแปรและชนิดข้อมูลใน progress.js', 'figures/code-js-vars.png') + """

2) เงื่อนไข (Conditional Statements) — คำสั่ง if / else if / else ใช้เลือกทางทำงานตามเงื่อนไข โครงงานใช้เงื่อนไขในเกือบทุกโมดูล ตัวอย่างที่เป็นหัวใจของระบบคือกติกาปลดล็อกด่านดังภาพที่ 2.7 ซึ่งใช้เงื่อนไขเรียงลำดับสามชั้น ได้แก่ กรณีไม่มีด่านนี้จริง กรณีเป็นด่านแรก และกรณีทั่วไปที่ต้องผ่านด่านก่อนหน้า

""" + fig('2.7', 'การใช้เงื่อนไข if/else ในกติกาปลดล็อกด่านของ progress.js', 'figures/code-js-condition.png') + """

3) การวนซ้ำ (Loops) — โครงงานใช้ลูป for แบบนับรอบสำหรับการวนตามลำดับที่ต้องรู้ตำแหน่ง เช่น การไล่สร้างจุดความก้าวหน้าของทุกด่านบนหน้าแผนที่ และใช้ forEach สำหรับการวนสมาชิกของอาร์เรย์โดยตรง เช่น การรวมรายการด่านที่ผ่านจากสองแหล่งข้อมูลเข้าด้วยกันในระบบซิงก์ ดังภาพที่ 2.8

""" + fig('2.8', 'การใช้ลูป for และ forEach ในโมดูลแผนที่และโมดูลซิงก์ข้อมูล', 'figures/code-js-loop.png') + """

4) ฟังก์ชันและฟังก์ชันเรียกกลับ (Functions and Callbacks) — ฟังก์ชันคือหน่วยของตรรกะที่นิยามครั้งเดียวแล้วเรียกใช้ซ้ำได้ จุดเด่นของ JavaScript คือฟังก์ชันเป็นค่าที่ส่งต่อให้ฟังก์ชันอื่นได้ (Callback) โครงงานใช้รูปแบบนี้เป็นแกนของโมดูลความก้าวหน้า ผู้เรียกส่งฟังก์ชันที่อธิบายการแก้ไขเข้ามา แล้วโมดูลจัดการโหลดและบันทึกให้เองโดยอัตโนมัติ ดังภาพที่ 2.9 รูปแบบนี้รับประกันว่าทุกการแก้ไขสถานะจะถูกบันทึกเสมอ ไม่มีทางลืมบันทึก นอกจากนี้ทุกโมดูลของโครงงานยังห่อโค้ดทั้งหมดไว้ในฟังก์ชันที่เรียกตัวเองทันที (IIFE) เพื่อไม่ให้ตัวแปรภายในรั่วออกไปปะปนกับโมดูลอื่น

""" + fig('2.9', 'ฟังก์ชันและ callback ในโมดูลความก้าวหน้า — ทุกการแก้ไขสถานะถูกบันทึกอัตโนมัติ', 'figures/code-js-function.png') + """

5) การจัดการเอกสารและเหตุการณ์ (DOM and Events) — JavaScript เข้าถึงองค์ประกอบบนหน้าเว็บผ่าน DOM (Document Object Model) และตอบสนองการกระทำของผู้ใช้ผ่านระบบเหตุการณ์ (Event) โครงงานใช้ document.getElementById ค้นหาองค์ประกอบ และ addEventListener ผูกการทำงานเข้ากับเหตุการณ์ต่าง ๆ ตั้งแต่การกดปุ่มรัน คีย์ลัด ไปจนถึงการสลับแท็บซึ่งใช้เป็นจังหวะบันทึกอัตโนมัติ ดังภาพที่ 2.10

""" + fig('2.10', 'การเข้าถึง DOM และการผูกเหตุการณ์ปุ่มรันโค้ดกับคีย์ลัดใน app.js', 'figures/code-js-dom.png') + """

6) การทำงานแบบไม่ประสานเวลา (Asynchronous Programming) — งานบางอย่างใช้เวลาไม่แน่นอน เช่น การติดต่อฐานข้อมูลบนคลาวด์ JavaScript จัดการด้วย Promise ซึ่งให้ผูกการทำงานต่อเมื่อสำเร็จ (.then) หรือผิดพลาด (.catch) โดยไม่ทำให้หน้าเว็บค้างรอ โครงงานยังใช้ setTimeout ทำเทคนิคหน่วงเวลา (Debounce) รวบการเปลี่ยนแปลงที่เกิดถี่ ๆ ให้เขียนขึ้นคลาวด์เพียงครั้งเดียว เพื่อประหยัดโควตาการเขียนของแผนบริการฟรี ดังภาพที่ 2.11

""" + fig('2.11', 'การหน่วงเวลาแบบ Debounce และการจัดการ Promise ในโมดูลซิงก์ข้อมูล', 'figures/code-js-async.png')

INS.append(('และเป็นรากฐานที่ทำให้การทดสอบอัตโนมัติในหัวข้อ 5.4 เป็นไปได้', JS))

INS.append(('ตามระบบที่อธิบายในหัวข้อ 1.9',
"\n\nการตั้งค่า Skulpt จริงของโครงงานแสดงดังภาพที่ 2.12 ซึ่งเห็นทั้งการต่อท่อผลลัพธ์เข้าหน้าต่างผลลัพธ์ การกำหนดขีดจำกัดเวลาสามวินาที และการรันแบบไม่ประสานเวลาที่ทำให้ input() หยุดรอผู้เรียนได้โดยเบราว์เซอร์ไม่ค้าง\n\n"
+ fig('2.12', 'การตั้งค่า Skulpt ใน runner.js ของโครงงาน', 'figures/code-skulpt-config.png')))

INS.append(('เมื่อผู้เรียนพิมพ์ตัวเปิด ระบบเติมตัวปิดให้ทันที ลดภาระการพิมพ์สัญลักษณ์ของผู้ที่ยังพิมพ์ไม่คล่อง',
"\n\nการตั้งค่า CodeMirror จริงของโครงงานแสดงดังภาพที่ 2.13\n\n"
+ fig('2.13', 'การตั้งค่า CodeMirror ใน app.js ของโครงงาน', 'figures/code-codemirror-config.png')))

INS.append(('ซึ่งเป็นการนำหลักวิศวกรรมซอฟต์แวร์มาตรฐานมาใช้จริงในโครงงานระดับนี้',
"\n\nองค์ประกอบทั้งหมดของระบบและความสัมพันธ์ระหว่างกันสรุปดังภาพที่ 2.14\n\n"
+ fig('2.14', 'แผนภาพองค์ประกอบของระบบ (System Component Diagram)', 'draw/system_components.png')))

INS.append(('แต่ยังรักษาสภาพแวดล้อมที่ปลอดภัยต่อการทดลองผิดพลาดตามหลักการในหัวข้อ 1.7 อีกทางหนึ่งด้วย',
"\n\nลำดับการทำงานทั้งหมดเมื่อผู้เรียนกดรันโค้ด ตั้งแต่การทำความสะอาดโค้ด การรันในขอบเขตจำกัด ไปจนถึงการตรวจเกณฑ์และการเปิดคำใบ้อัตโนมัติ สรุปเป็นผังงานดังภาพที่ 2.15\n\n"
+ fig('2.15', 'ผังการไหลของการทำงานเมื่อผู้เรียนกดรันโค้ด (Run-time Flow)', 'draw/run_flow.png')))

INS.append(('และหลักการคุ้มครองความเป็นส่วนตัวของผู้เรียนโดยการออกแบบ',
"\n\nลำดับการทำงานของสถาปัตยกรรม Local-first ตั้งแต่ผู้เรียนเปิดเว็บ การผสานข้อมูลเมื่อเข้าสู่ระบบ ไปจนถึงการบันทึกเมื่อความก้าวหน้าเปลี่ยน แสดงเป็นผังงานดังภาพที่ 2.16\n\n"
+ fig('2.16', 'ผังการทำงานของสถาปัตยกรรม Local-first และการซิงก์ข้ามอุปกรณ์', 'draw/sync_flow.png')))

INS.append(('โควตานี้จึงรองรับผู้เรียนได้จำนวนมากเพียงพอต่อขอบเขตของโครงงานโดยไม่มีค่าใช้จ่าย',
"\n\nหน้าคอนโซลจัดการโครงการ Firebase ของโครงงานแสดงดังภาพที่ 2.17\n\n"
+ ph('2.17', 'หน้าคอนโซลจัดการโครงการ Firebase ของโครงงาน', 'ภาพถ่ายหน้าจอ Firebase Console ของโครงการ learning-platform ที่แสดงเมนู Authentication และ Firestore Database')))

INS.append(('ซึ่งเพิ่มความซับซ้อนเกินความจำเป็นสำหรับข้อมูลที่มีลักษณะเป็นของผู้ใช้แต่ละคนแยกกันชัดเจนและไม่มีความสัมพันธ์ข้ามผู้ใช้ ตารางที่ 2-5 เปรียบเทียบคุณลักษณะของฐานข้อมูลสองแบบในบริบทของโครงงาน',
''))  # ไม่แทรก — คงไว้

INS.append(('](figures/table-2-5.png)',
"\n\nแม้ Firestore จะจัดเก็บข้อมูลแบบเอกสาร แต่เพื่อสื่อสารตรรกะของข้อมูลในเชิงเอกสารรายงาน คณะผู้จัดทำได้จำลองความสัมพันธ์ของข้อมูลเป็นแผนภาพอีอาร์ (Entity Relationship Diagram) ระดับแนวคิดดังภาพที่ 2.18 ผู้ใช้หนึ่งคนมีความก้าวหน้าหนึ่งชุดและมีโค้ดที่บันทึกได้หลายรายการ โดยทั้งความก้าวหน้าและโค้ดที่บันทึกอ้างอิงถึงด่านบทเรียนผ่านรหัสด่าน ส่วนการจัดเก็บจริงข้อมูลทุกกลุ่มถูกฝังรวมในเอกสารเดียวต่อผู้ใช้หนึ่งคน ตัวอย่างเอกสารจริงแสดงดังภาพที่ 2.19\n\n"
+ fig('2.18', 'แผนภาพความสัมพันธ์ของข้อมูลระดับแนวคิด (Conceptual ER Diagram)', 'draw/er_diagram.png')
+ "\n\n" + fig('2.19', 'ตัวอย่างเอกสารข้อมูลผู้เรียนหนึ่งคนใน Cloud Firestore (รูปแบบ JSON)', 'figures/fig-2-5-user-document-json.png')))

INS.append(('และใช้ร่วมกับส่วนขยายที่ให้บริการเว็บเซิร์ฟเวอร์ภายในเครื่องเพื่อทดสอบการทำงานของระบบแบบเรียลไทม์',
"\n\n" + ph('2.20', 'หน้าจอโปรแกรม Visual Studio Code ขณะพัฒนาโครงงาน', 'ภาพถ่ายหน้าจอ VS Code ที่เปิดโค้ดของโครงงาน เช่น ไฟล์ app.js หรือ missions.js')))

INS.append(('และใช้เป็นแหล่งเชื่อมต่อกับบริการเผยแพร่เว็บเพื่อนำระบบขึ้นให้บริการโดยอัตโนมัติ',
"\n\n" + ph('2.21', 'คลังซอร์สโค้ดของโครงงานบน GitHub', 'ภาพถ่ายหน้าจอ GitHub repository ของโครงงานที่แสดงรายการไฟล์และประวัติ commit')))

INS.append(('โดยมีการเตรียมสำเนาไลบรารีภายนอกไว้ในโครงงานเพื่อให้ระบบทำงานได้แม้ในสภาวะออฟไลน์',
"\n\n" + ph('2.22', 'แดชบอร์ดการเผยแพร่ระบบบน Vercel', 'ภาพถ่ายหน้าจอ Vercel dashboard ของโครงงานที่แสดงสถานะ deploy')
+ "\n\n" + ph('2.23', 'เว็บแอปพลิเคชันจริงที่เผยแพร่แล้วบนอินเทอร์เน็ต', 'ภาพถ่ายหน้าจอเว็บจริง (หน้า Welcome) ที่เปิดจากลิงก์ Vercel')))

count = 0
for anchor, text in INS:
    if not text:
        continue
    if anchor not in s:
        print('ANCHOR NOT FOUND:', anchor[:60]); continue
    s = s.replace(anchor, anchor + text, 1)
    count += 1
print('inserted', count, 'blocks')

# ---- เปลี่ยนเลขเดิม (แบบ 2-N) เป็นเลขใหม่ (แบบ 2.N) ตามลำดับสุดท้าย ----
tmap = {'2-1': '2.1', '2-2': '2.2', '2-3': '2.6', '2-4': '2.7', '2-5': '2.8', '2-6': '2.9', '2-7': '2.10'}
fmap = {'2-1': '2.1', '2-2': '2.2', '2-3': '2.3', '2-4': '2.24'}
s = re.sub(r'ตารางที่ 2-(\d+)', lambda m: 'ตารางที่ ' + tmap.get('2-' + m.group(1), '2-' + m.group(1)), s)
s = re.sub(r'ภาพที่ 2-(\d+)', lambda m: 'ภาพที่ ' + fmap.get('2-' + m.group(1), '2-' + m.group(1)), s)

# ---- ย้ายคำบรรยายของภาพเดิม 4 ภาพ (caption อยู่บน) ให้ไปอยู่ใต้ภาพ ----
pat = re.compile(r'(\*\*ภาพที่ 2\.\d+[^\n]*\*\*)\n\n(!\[[^\n]*\]\([^\n]*\))')
s, nswap = pat.subn(r'\2\n\n\1', s)
print('caption moved below for', nswap, 'figures')

# ---- กรอบแนวคิด: ชี้ไปไฟล์ drawio ที่ export แล้ว ----
s = s.replace('](figures/fig-2-4-conceptual-framework.png)', '](draw/conceptual_framework.png)')

io.open(f, 'w', encoding='utf-8').write(s)
print('saved')
